load("//tools/bazel_rules:index.bzl", "stylelint")

package(default_visibility = ["//:__subpackages__"])

stylelint(
    name = "stylelint",
    srcs = glob(["**/*.scss"]), # include all .scss files within this folder
    config = ":.stylelintrc"
)
